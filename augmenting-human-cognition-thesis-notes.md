# 博士论文笔记：《通过 AI 介导的闭环反馈增强人类认知》

## 基本信息

- 英文标题：Augmenting Human Cognition via AI-Mediated Closed-Loop Feedback
- 作者：Songlin Xu
- 学位：电气工程博士（通信理论与系统）
- 学校：加州大学圣地亚哥分校
- 年份：2026
- 导师：Xinyu Zhang
- 篇幅：PDF 共 179 页；学位论文正文页码为 1–128，之后为参考文献
- 方向：人机融合 / 认知增强 / 数字孪生 / 自适应反馈 / 强化学习

## 阅读进度

- [x] 学位论文摘要
- [x] 第 1 章：引言
- [x] 第 2 章：可感知干预的人类认知数字孪生
- [ ] 第 3 章：情境化学生学习模拟
- [ ] 第 4 章：理解反馈对行为调节的作用机制
- [ ] 第 5 章：基于 AI 反馈控制增强人类逻辑推理
- [ ] 第 6 章：结论与未来工作

## 论文整体结构

这篇论文提出了一套由模拟驱动的人机闭环方法：

```text
真实人类行为数据
        ↓
可感知干预的认知数字孪生
        ↓
在模拟环境中大规模训练反馈策略
        ↓
将自适应反馈部署给真实用户
        ↓
用观察到的行为机制改进模型和策略
        ↺
```

论文的中心观点是：AI 不应只代替人完成任务，还可以充当认知支架——观察人的表现、选择适当反馈，帮助人更有效地推理、学习和调节注意力。

四项主要技术工作共同组成一条完整管线：

| 章节 | 主要工作 | 在闭环中的作用 |
|---|---|---|
| 第 2 章 | CogReact：漂移扩散模型＋深度强化学习 | 模拟动态干预下的认知过程 |
| 第 3 章 | Classroom Simulacra＋可迁移迭代反思 | 将数字孪生扩展到长周期学生学习 |
| 第 4 章 | PeerEdu：异步同伴视线反馈 | 识别反馈如何真正改变人的行为 |
| 第 5 章 | 双 DRL 自适应时间压力反馈 | 在模拟环境中学习反馈策略并迁移给真人 |
| 第 6 章 | 综合结论与未来工作 | 扩展到传感、神经对齐与心理健康 |

---

## 学位论文摘要

### 研究问题

AI 正越来越多地进入教育和认知支持系统，但可扩展、具有明确方法论的人机融合系统仍不成熟。论文归纳了三个主要障碍：

1. **真人交互数据成本高。** 强化学习反馈控制器需要大量连续交互，但反复让真实用户参与探索既昂贵又缓慢，还会造成疲劳甚至潜在伤害。
2. **反馈机制尚未被充分理解。** 同伴注意力等社会信号能够改变学习行为，但人可能选择性地理解、接受或抵制这些信号，而不是作出统一反应。
3. **自适应反馈缺乏通用控制方法。** 现有系统通常采用固定反馈或短视规则，没有针对完整认知轨迹优化干预时机。

### 解决方案

论文将三个组成部分整合起来：

- **数字孪生建模：** 模拟人类认知在干预下如何演化。
- **实证行为分析：** 通过真人实验识别反馈如何改变注意力、策略和表现。
- **强化学习反馈控制：** 先让自适应策略与模拟认知智能体交互训练，再部署给真实用户。

### 主要系统

- **CogReact：** 将漂移扩散理论与深度强化学习结合，模拟动态反馈下的认知过程。
- **Classroom Simulacra：** 使用可迁移迭代反思增强 LLM 学生模拟器，建模情境化学习轨迹。
- **PeerEdu：** 研究同伴视觉注意反馈如何调节在线学习行为。
- **双 DRL 反馈框架：** 让调节智能体与模拟认知智能体交互训练，降低直接用真人进行在线强化学习探索的需求。

### 核心主张

这些系统共同构成一套可扩展的方法，用于建模、研究和优化面向认知增强的自适应人机系统。

---

## 第 1 章：引言

### 1.0 从任务自动化转向认知增强

人类认知包括感知环境、不确定条件下推理、作出决策、调节注意力、从经验中学习，以及把知识迁移到新环境等能力。现代 AI 在语言生成、推理和自动分析方面发展迅速，但多数系统围绕**任务自动化**设计，即让模型代替用户解决问题。

作者认为，自动化虽然能提高短期效率，却可能带来三类长期风险：

- 用户进行高投入推理和发展技能的机会减少。
- 用户可能获得正确答案，却没有理解原理或形成可迁移的策略。
- 对 AI 的依赖可能削弱适应能力、专业能力保持、批判性监督和人的自主性。

论文因此区分了两种范式：

| 任务自动化 | 认知增强 |
|---|---|
| AI 完成任务 | AI 帮助人把任务完成得更好 |
| 优化眼前的任务结果 | 优化推理、学习、注意力和策略 |
| 可能把人移出任务闭环 | 让人持续保持认知参与 |
| AI 是执行者 | AI 是反馈控制器或认知支架 |

作者的价值立场是：认知增强应成为人脑的必要“外骨骼”。随着机器能力不断增强，人机系统应主动保护和强化人的智力，而不应只追求自动化程度。

### 1.1 动态干预下的认知模拟

#### 为什么需要模拟器

自适应反馈系统需要学习一系列连续决策：

- 现在是否需要干预；
- 应当提供什么反馈；
- 反馈应当多强、持续多久；
- 应当何时撤回反馈；
- 当前干预会如何影响之后的表现。

强化学习适合优化这类决策，但需要大量交互数据。直接用真人训练会产生数据不足问题：

- 每次真人实验的速度很慢；
- 大规模研究成本很高；
- 参与者容易疲劳；
- 每次修改模型或超参数都可能需要重新开展用户实验；
- 训练初期的随机探索可能降低用户表现或制造压力。

计算认知数字孪生可以充当模拟环境。反馈控制器先在模拟中进行探索和学习，形成相对成熟的策略后再迁移给真实用户。

#### 为什么普通预测模型不够

许多认知模型和机器学习模型将行为视为静态映射：

```text
任务刺激 → 最终准确率或响应时间
```

闭环干预需要的是动态模型：

```text
任务＋个人状态＋反馈历史
    → 下一步认知状态与行为
    → 对后续干预的未来反应
```

模型需要表示：

- 随时间演化的认知过程；
- 反馈的时间与强度；
- 个体之间的差异；
- 不同刺激或题目的难度；
- 干预如何扰动中间认知轨迹。

#### 本文中“数字孪生”的含义

这里的数字孪生并不是一个人的完整大脑复制品，而是一个针对特定任务的行为模型，用于近似人的反应轨迹以及反馈会如何改变这条轨迹。因此，其可信程度必须结合明确的任务、行为、人群和干预方式来判断。

### 1.2 理解反馈调节机制

人类认知会受到社会和情境信号影响。在教育环境中，同伴行为、共同注意和教学节奏都可能改变学生如何分配注意力和努力。

但作者反对“反馈越强、越直接，效果就越好”的简单假设。人具有**适应性自主能力**：

- 会判断反馈是否与当前任务相关；
- 会选择性地跟随或忽略外部信号；
- 会结合自身目标和当前认知状态解释反馈；
- 即使个人策略与反馈不同，也可能继续保持已经有效的策略。

这形成了一项重要设计约束。如果系统把服从反馈等同于成功，就可能：

- 强迫所有用户采用同一种注意模式；
- 打断原本有效的个人策略；
- 把有价值的偏离错误识别为注意力不集中；
- 优化行为一致性，而非真正的学习结果。

论文因此认为，反馈控制必须建立在实证行为机制之上。第 4 章通过分析视线、注意力、困惑、课程跟随程度和学习结果来落实这一思想，而不只观察最终考试成绩。

### 1.3 用于反馈控制的自适应认知策略

多数现有反馈系统使用固定或短视规则，例如检测到参与度下降就立刻提醒。这类策略只针对当前状态作出反应，没有优化完整认知过程。

反馈可能产生累积且非线性的影响。例如，时间压力在开始时可能提高速度和唤醒水平，但过强或持续过久时，会增加焦虑、干扰用户并损害准确率。因此，最优行动既取决于当前状态，也取决于对未来结果的预期。

强化学习为长期连续优化提供了形式化框架。论文使用两阶段方法解决强化学习对真人数据的需求：

1. 利用真实行为数据训练一个可感知干预的认知模拟器。
2. 让调节智能体与模拟器进行大量交互，在真人测试之前学习反馈策略。

由此形成论文的核心“模拟到现实”闭环：

```text
行为观测
    → 认知模拟器
    → 在模拟环境中学习策略
    → 向真人提供反馈
    → 获得新的行为观测
```

### 1.4 学位论文的主要贡献

#### 贡献一：可感知干预的数字孪生

**CogReact** 将漂移扩散模型（Drift-Diffusion Model，DDM）的可解释性，与深度强化学习对非线性关系和连续过程的建模能力结合起来。它旨在模拟动态时间压力如何扰动证据积累、决策和响应时间，同时保留个体差异和刺激差异。

**Classroom Simulacra** 将数字孪生思想扩展到教育环境。它使用 LLM 和可迁移迭代反思模块，根据少量示例和复杂课程上下文，模拟长时间跨度的学习轨迹及学生之间的相关关系。

这两项工作提供了模拟环境，使研究者不必反复让真人接触尚未成熟的反馈策略，也可以训练和评估自适应系统。

#### 贡献二：通过实验识别反馈机制

**PeerEdu** 在大规模在线学习实验中研究异步同伴视觉注意反馈。它的重要概念性发现是：同伴反馈不会简单地产生行为趋同。学生会根据自己的学习需求调整注意力，更紧密地跟随同伴视线也不一定会带来更好的结果。

这项工作为数字孪生和控制策略提供了行为约束：有效的认知增强应支持适应性自我调节，而不是强迫所有人采用一致行为。

#### 贡献三：由模拟驱动的反馈控制

**双 DRL 框架**将两个角色分开：

- 模拟智能体根据任务和反馈条件模仿人的准确率与响应时间；
- 调节智能体决定什么时候施加时间压力反馈。

调节智能体通过与模拟器反复交互进行训练，避免直接在真实用户身上进行昂贵且不安全的随机探索。完成训练后，再通过真人实验评估所得策略。

这种方法将反馈设计从人工编写规则转化为对人类长期表现的连续决策优化。

---

## 第 2 章：可感知干预的人类认知数字孪生（CogReact）

### 2.1 本章要解决的问题

本章关注的不是“给定任务后人会做出什么反应”，而是：**当环境反馈在过程中动态变化时，如何细粒度地模拟它对人类认知过程的扰动？**

以时间压力为例，反馈可能逐帧变化，也可能只在部分试次出现，并且还可能根据用户过去的表现自适应改变。把它简单编码成“有压力/无压力”会丢失反馈的时间结构和强度变化。

作者提出 CogReact（Cognitive Reaction），把漂移扩散模型（DDM）的认知过程解释能力，与深度强化学习（DRL）的序列决策和函数逼近能力结合起来，建立一个能感知动态干预的认知数字孪生。

### 2.2 相关工作与研究缺口

- **认知过程模型：** BEAST、DDM 等模型用封闭形式描述决策过程。DDM 把决策看成证据不断积累直到越过边界，因此能够自然表达速度—准确率权衡，但通常更偏向对已有决策进行后验估计。
- **机器学习认知模拟：** RNN、卷积模型、潜在动力学模型等已经用于视觉认知、分类、决策、游戏策略和反应时间预测，但多数模型针对理想、静态条件。
- **本章指出的缺口：** 现有方法较少刻画外部刺激随时间变化、并根据用户状态影响中间认知轨迹的情况。仅预测最终准确率或响应时间，不足以支持反馈控制。

### 2.3 数据集与任务设置

#### 数学逻辑推理任务

每个试次给参与者两个两位数和一个一位数，参与者判断两个两位数相减后的结果是否能被该一位数整除，并做出二元选择。系统记录选择准确率和响应时间。

#### 新收集的人类数据

- 有效反应：**21,157 条**。
- 参与者：**44 人**。
- 时间压力条件：
  - **None：** 所有试次无时间压力；
  - **Static：** 每个试次都施加时间压力；
  - **Random：** 每个试次以 50% 概率施加时间压力；
  - **Rule：** 根据用户过去表现，用规则自适应决定是否施加时间压力。
- 实验经过机构 IRB 审批。
- 由于实验指示参与者优先保证准确率，外部刺激对准确率的影响较小，因此本章主要模拟**响应时间**，而不是把准确率作为主要调节对象。

#### 用于训练逻辑推理智能体的独立数据

作者另外遍历三位数字的组合，得到 **20,414 个样本**，按 80%/20% 划分训练集和测试集，用于训练数学逻辑推理 LSTM。这个数据集与真人行为数据独立，避免任务求解器直接从真人反应中学习。

### 2.4 CogReact 的四步框架

```text
数学逻辑推理 LSTM 智能体
        ↓
把智能体内部特征映射到真人的基线选择/响应时间（SVM）
        ↓
用 DDM 解码证据积累过程，得到细粒度认知轨迹
        ↓
让 DRL 按时间压力视频帧逐步调节证据积累过程
        ↓
输出受到干预后的响应时间
```

1. **Math Agent：** 训练 LSTM 数学答题智能体，使它先学会解决任务。其隐藏层输出被视为题目难度和逻辑结构的表示。
2. **Transfer to Humans：** 用 SVM 的分类器（SVC）和回归器（SVR）把智能体特征映射为真人在理想条件下的选择和响应时间，得到不受时间压力影响的基线表现。
3. **DDM 解码：** 根据基线预测反推出 DDM 的边界阈值和积累时间，把一次回答拆分为连续的证据积累步骤。
4. **Hybrid DRL：** 把时间压力视频切成与证据积累步骤对应的帧。每一帧输入 DRL，DRL 选择正向、中性或负向偏置，改变证据轨迹到达边界的时间，最后得到干预后的响应时间。

这种结构的关键是：DRL 不再直接输出一个黑盒的响应时间变化，而是逐步修改一个具有认知含义的中间过程。

### 2.5 主要评估设置、指标与 baseline

#### 输入表示的五种设置

作者比较了任务刺激和反馈刺激的不同表示方式：

1. 任务视频 + 反馈视频；
2. 编码字符串任务 + 反馈视频（CogReact 的主要设置）；
3. 数值任务 + 反馈视频；
4. 数值任务 + 数值反馈；
5. 编码字符串任务 + 数值反馈。

视频输入设置使用 hGRU、带预训练视觉模型的 LSTM、带预训练 3D ResNet 的 MLP 等近期模型作为 baseline；其他输入形式则使用对应模态下的 SVM、LSTM、Transformer、MLP、随机森林、决策树和线性回归等模型。

#### 评估指标

- **MAPE：** 主要指标，用于衡量模拟响应时间与真人响应时间的差异。作者认为响应时间存在很强的个体差异，因此采用 MAPE 而不是 MSE，并在深度模型训练中也使用 MAPE loss。
- **Pearson 相关系数：** 衡量模型是否捕捉到了真人响应时间随试次变化的趋势。
- **选择预测指标：** Accuracy、F1-score、Precision、Recall。
- **统计检验：** Kolmogorov–Smirnov 检验和置换检验，避免依赖正态分布假设；多处比较报告对 baseline 的差异达到 `p < 0.001`。

作者使用四种训练/测试层级检查泛化性：

- **General-level：** 全体数据按 80%/20% 划分；
- **Group-level：** 在每一种时间压力组内训练和测试；
- **Individual-level：** 针对单个参与者训练和测试，考察个性化建模；
- **LOPO-level：** Leave-One-Participant-Out，把同组一个参与者全部数据作为测试集，其余参与者作为训练集，以保留并检验跨试次的时间趋势。

### 2.6 主要实验结果

#### 2.6.1 端到端响应时间模拟

CogReact 在字符串任务/视频反馈和数值任务/数值反馈两类设置中表现最好；其中数值编码的 Type IV 在推理时更高效。对输入模态匹配后的模型比较显示，CogReact 的 MAPE 显著低于大多数 baseline；在 Type IV 中也显著优于 LSTM/Transformer（`p < 0.001`）。

总体 MAPE 的均值为 0.2703，但方差较大。固定用户、跨试次平均后，MAPE 均值降为 0.1388；固定试次、跨用户平均后为 0.1403，说明较大的误差主要来自“用户差异 × 题目差异”的交互，而不是单一噪声源。

#### 2.6.2 Math Agent 特征的消融

将 SVM 的输入从 Math Agent 隐藏特征替换为原始三位数字或数字的 one-hot 编码后，性能变差。使用 Math Agent 特征时：

- 选择预测 Accuracy：**0.9613**；
- 选择预测 F1：**0.8996**；
- 响应时间估计 MAPE：**0.3652**。

这说明题目求解器的内部表示比原始数字更能反映题目难度和潜在认知负荷。

#### 2.6.3 逻辑推理智能体本身是否可靠

LSTM 的输出层神经元数量比较了 32、64、128、256 四种设置。256 个神经元时，训练损失达到 0.0001，训练准确率为 100%，测试集总体准确率为 **99.93%**；其他设置的测试准确率也超过 95%。因此，Math Agent 能够稳定解决算术任务，为后续抽取题目特征提供基础。

#### 2.6.4 DDM 对 DRL 的作用

作者构造了两个消融 baseline：

- **Pure DRL：** 不使用 DDM，不逐帧处理时间压力视频，而是直接针对整个试次输出一个响应时间变化；
- **SVM-only：** 删除整个 DRL 环节，只用 SVR 预测响应时间。

Hybrid DRL 在大多数个体、组别、总体和 LOPO 测试中都比 Pure DRL 取得更低的 MAPE 和更高的 Pearson 相关，并且比 SVM-only 更能恢复响应时间的时间趋势。

训练效率方面，Pure DRL 约需 **38.30 分钟**收敛，Hybrid DRL 约需 **4.42 分钟**，约为前者的十分之一。这里不能直接比较训练 step 数，因为 Hybrid DRL 的一个 step 对应一个时间压力帧，而 Pure DRL 的一个 step 对应一个完整试次。

#### 2.6.5 可解释性结果

Hybrid DRL 可以输出每个试次的“时间压力作用轨迹”：即 DRL 动作经过累积后，对证据积累过程造成的变化。

- Random 组的轨迹最集中，动作轨迹标准差最低；
- Random 组的时间压力作用平均值和斜率最高；
- None 和 Static 组的轨迹更分散，可能反映参与者较快适应了固定或不存在的时间压力；
- 结果与真人探索实验一致：Random 组的响应时间下降更明显，调节效应持续时间更长。

因此，模型不仅能说“响应时间变了多少”，还能够解释哪一种反馈模式更持续地调节了证据积累过程。

### 2.7 跨任务泛化实验

作者把 CogReact 迁移到两个公开数据集：

- **CPC18：** 决策任务；
- **PeerEdu：** 学习任务。

由于这两个数据集没有视频输入，作者使用与 Type IV/Type V 对齐的 baseline；在 PeerEdu 中，文本输入统一使用 OpenAI `text-embedding-3-small` 编码。

图 2.9 的 MAPE 结果如下（越低越好）：

| 模型 | 决策任务 | 学习任务 |
|---|---:|---:|
| Hybrid DRL | **0.357** | **0.950** |
| Pure DRL | 1.616 | 2.682 |
| Unencoded Hybrid DRL | 0.360 | 0.956 |
| Encoded SVM | 0.558 | 1.174 |
| SVM | 0.385 | 1.167 |
| LSTM | — | 1.274 |
| Transformer | — | 1.207 |
| MLP | — | 1.204 |
| Random Forest | 0.512 | 1.224 |
| Decision Tree | 0.536 | 1.222 |
| Linear Regression | 0.632 | 1.218 |

Hybrid DRL 在两个任务上都是最低 MAPE。消融结果表明：删除 DDM 的 Pure DRL 误差显著增加，通常比所有 baseline 更差；删除第一步的 Unencoded Hybrid DRL 只造成轻微下降，说明 DDM 在反馈调节建模中作用更突出。

一个有意思的现象是，Encoded SVM（风险/LLM agent + SVM）反而比普通 SVM 更差，说明这些表示不一定适合直接做静态回归；但它们加入 Hybrid DRL 的观察空间后仍然有帮助，表明“特征是否有用”取决于它被放在何种动态建模结构中。

### 2.8 DDM + 深度学习是否足够？

作者还把 DDM 直接接入 LSTM、Transformer、MLP，并增加与 Hybrid DRL 网络结构相同但不使用 RL 的 MLPv2，用来区分“深度网络 + DDM”和“强化学习 + DDM”的贡献。

三项任务的 MAPE 如下：

| 模型 | 数学任务 | 决策任务 | 学习任务 |
|---|---:|---:|---:|
| Hybrid DRL | **0.270** | **0.357** | **0.950** |
| LSTM | 0.306 | 0.461 | 1.274 |
| LSTM + DDM | 0.331 | 0.480 | 1.211 |
| Transformer | 0.305 | 0.535 | 1.207 |
| Transformer + DDM | 0.331 | 0.444 | 1.198 |
| MLP | 0.329 | 0.476 | 1.204 |
| MLPv1 + DDM | 0.312 | 0.463 | 1.236 |
| MLPv2 + DDM | 0.309 | 0.468 | 1.186 |

结果说明，单纯加入 DDM 有时能改善普通深度学习模型，但整体仍显著不如 Hybrid DRL（`p < 0.001`）。因此，CogReact 的收益不能归因于“深度学习 + DDM”这一组合本身，RL 的序列交互和动作选择是不可替代的组成部分。

### 2.9 本章结论与限制

本章证明了 CogReact 可以在数学推理、决策和学习三类任务，以及数值、文本和连续行为等不同反馈模态下，模拟环境刺激对认知过程的影响。其主要优势有三点：

1. **准确性：** 在响应时间模拟和跨任务泛化中降低 MAPE；
2. **效率：** DDM 将长序列反馈拆成认知步骤后，Hybrid DRL 更快收敛；
3. **可解释性：** 能输出反馈如何逐步改变证据积累轨迹。

限制是本章重点仍是响应时间，对其他认知指标覆盖不足。作者提出后续可以扩展到更多任务，让任务求解器特征与真人行为建立映射，并根据不同反馈动态调整 DRL 的观察空间和动作空间。

### 2.10 对本论文主线的意义

第 2 章给整篇论文提供了“可在模拟器中训练反馈控制器”的技术基础。它最重要的设计选择不是把 DDM 当作一个单独的预测器，而是让 DDM 成为 DRL 的中间状态结构：

```text
可解释的认知过程（DDM）
        +
面向长期交互的控制器（DRL）
        ↓
可解释、可训练、可迁移的认知数字孪生
```

但也要注意：本章的外部效度仍依赖任务和刺激的覆盖范围。它证明了“动态反馈可以被建模”，并不自动证明模型已经完整复现了人的认知机制，更不等于已经证明反馈能带来长期能力提升。

### 2.11 本章阅读后的问题

- Hybrid DRL 在跨参与者迁移时是否有明确的不确定性估计？
- MAPE 对极短响应时间是否敏感，作者是否报告了不同时间尺度下的误差？
- Random 组的时间压力效果更强，是因为不可预测性、压力强度，还是参与者形成了不同的策略？
- 在 CPC18 和 PeerEdu 上，Hybrid DRL 的优势有多少来自任务特定的 embedding 或数据预处理？
- 从本章的模拟器到第 5 章真人反馈控制，中间的模拟到现实差距如何被校准？

## 各章节之间的关系

### 第一层：能否模拟干预下的认知？

第 2、3 章分别为短期推理和长周期学生学习建立数字孪生。

### 第二层：模拟器应该包含什么行为机制？

第 4 章观察人如何实际适应同伴注意反馈，并说明简单的“服从即有效”假设为什么不成立。

### 第三层：能否在模拟中学习反馈策略？

第 5 章让强化学习控制器与认知模拟器交互训练，并把所得策略迁移到真实用户。

### 第四层：长期研究计划是什么？

第 6 章把框架扩展到多模态传感、神经对齐和学生心理健康支持。

因此，统一这篇论文的主要是一种方法论，而不是某一种算法：

> 建立数字孪生，识别真实反馈机制，在模拟环境中学习策略，通过真人实验验证，再用新的行为观察改进下一轮模型。

---

## 第 1 章批判性阅读

### 认知增强与表现增强并不相同

论文把目标描述为增强人的长期认知能力，但实证章节主要测量响应时间、准确率、视线、注意力、困惑和测试成绩。这些指标可以证明短期行为受到调节，却不能自动证明：

- 移除反馈后，提升仍然存在；
- 用户形成了可迁移的推理策略；
- 长期技能保持得到改善；
- 用户自主能力得到增强，而不是更加依赖系统。

因此，后续阅读需要持续区分两类主张：

1. **表现增强：** 用户在反馈存在时把当前任务做得更好。
2. **能力发展：** 用户的底层能力得到提升，并能迁移到干预之外。

目前这篇论文对第一项提供的直接证据明显强于第二项。

### 数字孪生的保真度是整个闭环的瓶颈

强化学习控制器的可靠程度不会超过其模拟环境。如果数字孪生存在系统性误差，控制器可能学会利用模拟器漏洞，而不是学到真正对人有效的策略。后续章节需要重点检查：

- 模型复现的是个体行为，还是只有群体平均趋势；
- 训练数据是否真正覆盖了各种干预条件；
- 校准参与者与测试参与者是否分离；
- 是否建模了不确定性和个体内部变异；
- 策略从模拟环境迁移给真人后是否仍然有效。

### 人类认知不是普通的控制对象

“调节”“控制”“干预”等工程语言容易遮蔽自主性和知情同意问题。一个成功的反馈系统不应只优化可观测指标，还应该考虑：

- 用户是否理解系统正在实施的干预；
- 用户能否拒绝或调整反馈；
- 更快的表现是否以更高压力为代价；
- 系统优化的是用户目标还是平台目标；
- 不同人群是否受到不同影响；
- 长期使用究竟增强还是削弱自我调节能力。

第 1 章通过“适应性自主能力”部分触及了这一问题，在后面的 PeerEdu 章节中尤其重要。

---

## 第一轮阅读结论

### 一句话总结

这篇论文提出将可感知干预的数字孪生、行为实验和强化学习反馈控制结合起来，构建能够增强人的认知表现、而不是简单替人完成任务的人机系统。

### 后续章节需要检验的三个命题

1. 数字孪生必须模拟干预的时间效应，而不能只预测最终回答。
2. 有效的反馈策略必须尊重人的主动适应，不能只优化行为一致性。
3. 在模拟中有效的策略，仍必须证明它能够安全、可靠地迁移到真人。

### 后续阅读问题

- CogReact 如何区分个体差异、题目难度和干预效应？
- Classroom Simulacra 能否预测真正的新学习轨迹，还是主要复现示例中已有的模式？
- 有什么证据能说明 TIR 学到了可迁移知识，而不只是生成了更长的推理过程？
- PeerEdu 的效果究竟来自同伴信息、注意力提升、社会压力，还是界面视觉显著性？
- 双 DRL 实验中的模拟到现实差距有多大？
- 这些实验能否证明长期认知能力提升，还是只能证明暂时性的表现变化？
